node_modules/
dist/

# Generated pages — rendered from content/ + templates/ by `npm run render`
/*.html
/our-school/
/our-community/
/curriculum/
/cocurricular/
/join-us/
/international-students/

# Build/scratch artefacts
carlito.css
lh-*.json

# Media IS tracked (public/assets/**) so that CMS uploads and the existing
# photography deploy with the site. ~100MB across ~690 files — well inside
# Cloudflare Pages / Netlify limits.
